import { ResponsePatternPipe } from './response-pattern.pipe';

describe('ResponsePatternPipe', () => {
  it('should be defined', () => {
    expect(new ResponsePatternPipe()).toBeDefined();
  });
});
